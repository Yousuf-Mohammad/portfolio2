# EmailJS configuration (https://dashboard.emailjs.com)
# These are public client-side identifiers. Lock the public key down by
# enabling "Allowed origins" in the EmailJS dashboard so it only works
# from your deployed domain.
NEXT_PUBLIC_EMAILJS_SERVICE_ID=your_service_id
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=your_template_id
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=your_public_key
